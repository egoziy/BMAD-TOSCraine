namespace Rtg.Tests;

public class SkeletonTests
{
    [Fact]
    public void SolutionBuilds()
    {
        // Story 1.1 sentinel test — confirms the test runner discovers and runs at least one test.
        // Replaced by real tests in subsequent stories.
        Assert.True(true);
    }
}
