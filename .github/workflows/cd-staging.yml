name: cd-staging

on:
  push:
    branches: [main]

jobs:
  placeholder:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Real deploy logic implemented in Story 8.x (self-hosted runner + Windows Service restart)"
