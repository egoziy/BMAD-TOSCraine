name: cd-production

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  placeholder:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Real deploy logic implemented in Story 8.x (manual approval gate + self-hosted runner)"
