# PostgreSQL configuration for craines-tos local dev.
# Story 1.2 fills this with `wal_level=replica` + tuning + connection pooling settings.
# This placeholder lets docker-compose.dev.yml reference the file without breaking.

listen_addresses = '*'
